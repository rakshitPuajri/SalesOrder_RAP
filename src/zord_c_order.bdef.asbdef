projection;
strict ( 2 );
use draft;

define behavior for ZORD_C_ORDER alias Order
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _items { create; with draft; }
}

define behavior for ZORD_C_ORDERITEM alias OrderItems
{
  use update;
  use delete;

  use association _header { with draft;}
}